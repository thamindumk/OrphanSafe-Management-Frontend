version: '3.1'
services:
  management-frontend:
    image: sandevdewthilina/orphange-management-frontend:latest
    # command: npm run dev
    # volumes:
    #   - .:/usr/app/
    #   - /usr/app/node_modules
    ports:
      - "13000:13000"
  #   depends_on:
  #     - postgres
  
  # postgres-central-db:
  #   image: postgres:9.6.2-alpine
  #   environment:
  #     POSTGRES_USER: todoapp
  #     POSTGRES_DB: todos
